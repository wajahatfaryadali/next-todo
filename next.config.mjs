/** @type {import('next').NextConfig} */
const nextConfig = {
    // makeing it false becuase useeffect get called twice and i have to make an api call
    reactStrictMode: false,
};

export default nextConfig;
